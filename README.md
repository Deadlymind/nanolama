# 🦙 nanolama

> A tiny master that forges skill-equipped repos.

[![License: MIT](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
![Skills: 30](https://img.shields.io/badge/skills-30-blue.svg)
![Stack: Django 5.2 · Next.js 16](https://img.shields.io/badge/stack-Django%205.2%20%C2%B7%20Next.js%2016-informational.svg)
![PRs welcome](https://img.shields.io/badge/PRs-welcome-brightgreen.svg)

**nanolama is a portable [Claude Code](https://code.claude.com) skills library — a
plugin, not an application.** Install it and every session already knows how this
house stack is built: multi-tenant Django/DRF, cookie JWT, Celery, Channels,
Next.js/React Query/Zod, AWS, and the review / security / testing discipline that
keeps it correct. The product *is* the curated catalog below.

Every skill teaches a **reusable pattern plus an "adapt to your repo" note** — it is
model-agnostic and works on any project on the stack, not one codebase. Tenant
isolation and security are the north star.

## Install

This repo is both a plugin **and** its own marketplace, so you can use it locally or
publish it.

```bash
# Try it locally (no install), from the repo root:
claude --plugin-dir .

# Or register the local marketplace and install:
/plugin marketplace add .
/plugin install nanolama@nanolama-marketplace
```

Once you push this repo to GitHub as `<owner>/nanolama`:

```bash
/plugin marketplace add <owner>/nanolama
/plugin install nanolama@nanolama-marketplace
```

Skills auto-trigger from their descriptions; you can also run any of them explicitly
as `/nanolama:<skill>`.

## The stack these skills target (baseline)

- **Backend** — Django 5.2 LTS · DRF 3.16 · PostgreSQL · Python 3.12+
- **Auth** — cookie-based JWT (`djangorestframework-simplejwt` + `dj-rest-auth`)
- **Multi-tenant** — non-null `entreprise` FK on every business model · fail-closed
  auto-filtered ViewSets · `{resource}_{action}` RBAC
- **Async / real-time** — Celery 5.6.3 + Redis · Django Channels 4.2+ on ASGI
  (Daphne in dev, Uvicorn/Gunicorn in prod)
- **AI** — Anthropic Claude (agentic tools + OCR extraction) + Tavily
- **Frontend** — Next.js 16 · React 19 · TanStack Query 5 · Zustand · Zod 4 ·
  Tailwind v4 · shadcn/ui · next-intl (fr/en/ar, Arabic RTL)
- **Infra** — AWS Elastic Beanstalk · Amplify · RDS Postgres · ElastiCache Redis ·
  S3 · GitHub Actions CI

> These versions are the **baseline as of 2026-07-16**, not a permanent truth. The
> [`version-check`](skills/version-check/SKILL.md) skill has you deep-research the current
> stable/LTS release and its support window before you install, pin, or upgrade anything —
> Django 5.2 is still the current LTS, but the rest of the stack keeps moving.

## What's inside

<!-- BEGIN SKILLS TABLE (generated by scripts/generate_readme.py) -->

_30 skills across 7 categories._

### 🗄️ Backend — Django & DRF

| Skill | What it does |
| --- | --- |
| [`celery-tasks`](skills/celery-tasks/SKILL.md) | Hardened background jobs: time limits, retries/backoff, pass ids not objects, idempotency. |
| [`codebase-guide`](skills/codebase-guide/SKILL.md) | How a feature is built end to end: model to serializer to viewset to urls to tests. |
| [`db-concurrency`](skills/db-concurrency/SKILL.md) | Money/counter safety: select_for_update + atomic + F(); Decimal not float. |
| [`drf-api`](skills/drf-api/SKILL.md) | Serializers, viewsets, routers, pagination, export actions, the OpenAPI schema. |
| [`migrations`](skills/migrations/SKILL.md) | Safe schema + data migrations: apps.get_model, idempotent RunPython, tenant backfills. |
| [`multi-tenancy`](skills/multi-tenancy/SKILL.md) | entreprise FK on every business row; fail-closed auto-filtered ViewSets. |
| [`rbac-permissions`](skills/rbac-permissions/SKILL.md) | {resource}_{action} roles; secure-by-default checks; guard custom @actions. |
| [`websockets-channels`](skills/websockets-channels/SKILL.md) | Channels consumers, cookie-JWT WS auth, tenant-scoped groups, send-after-commit. |

### 🎨 Frontend — Next.js & React

| Skill | What it does |
| --- | --- |
| [`i18n-rtl`](skills/i18n-rtl/SKILL.md) | next-intl fr/en/ar; every string via t(); logical CSS + Arabic RTL. |
| [`nextjs-module`](skills/nextjs-module/SKILL.md) | Feature pattern: lib/api to query-keys to hooks to pages; the shared fetch client. |
| [`react-query`](skills/react-query/SKILL.md) | Server state: query keys, staleTime, invalidation after mutations. |
| [`tailwind-shadcn`](skills/tailwind-shadcn/SKILL.md) | Tailwind v4 semantic tokens + shared shadcn components; reuse, do not rebuild. |
| [`zod-forms`](skills/zod-forms/SKILL.md) | Typed schemas + forms; the Select-all and z.coerce.number traps; i18n messages. |
| [`zustand-state`](skills/zustand-state/SKILL.md) | Client stores with persist/partialize (ids not objects) and the hydration guard. |

### 🔗 Full-stack contract

| Skill | What it does |
| --- | --- |
| [`drf-zod-contract`](skills/drf-zod-contract/SKILL.md) | Keep a DRF serializer and its Zod 4 schema / TanStack hook in sync, end to end. |

### ☁️ Infra & DevOps — AWS + CI

| Skill | What it does |
| --- | --- |
| [`aws-services`](skills/aws-services/SKILL.md) | Provision RDS Postgres, ElastiCache Redis, private S3 with an IAM instance role. |
| [`ci-cd`](skills/ci-cd/SKILL.md) | GitHub Actions gates: check, makemigrations --check, tests, lint, typecheck, build. |
| [`deploy-aws`](skills/deploy-aws/SKILL.md) | Elastic Beanstalk + Amplify runbook: Procfile, migrate leader-only, env vars, rollback. |

### 🤖 AI integration

| Skill | What it does |
| --- | --- |
| [`ai-integration`](skills/ai-integration/SKILL.md) | Claude tool-use + agentic loops; the write-tool safety contract; OCR extraction; evals. |

### 🛡️ Quality & Security

| Skill | What it does |
| --- | --- |
| [`code-review`](skills/code-review/SKILL.md) | Review the diff for correctness + reuse/simplify/efficiency; tenant/RBAC gates; ranked. |
| [`debug`](skills/debug/SKILL.md) | Reproduce, isolate, hypothesize, fix root cause, verify + add a regression test. |
| [`dependency-audit`](skills/dependency-audit/SKILL.md) | Audit Python + JS deps for CVEs and drift: pip-audit, pnpm audit, lockfile hygiene. |
| [`perf-review`](skills/perf-review/SKILL.md) | Find N+1 / unbounded queries; select_related, prefetch_related, bounded dashboards. |
| [`security-review`](skills/security-review/SKILL.md) | Audit for tenant isolation, RBAC gaps, PII leaks, upload validation, secrets. |
| [`verify`](skills/verify/SKILL.md) | Drive the change end to end and observe real behavior before claiming done. |
| [`version-check`](skills/version-check/SKILL.md) | Deep-research the current stable/LTS version and support window before installing or pinning. |
| [`write-tests`](skills/write-tests/SKILL.md) | Django TestCase with tenant fixtures + the isolation test, and vitest fetch-mocking. |

### 🧰 Workflow & Meta

| Skill | What it does |
| --- | --- |
| [`changelog`](skills/changelog/SKILL.md) | Maintain a Keep a Changelog CHANGELOG.md; roll Unreleased into a release. |
| [`commit-message`](skills/commit-message/SKILL.md) | Write Conventional Commits from the staged diff; correct type/scope/subject. |
| [`skill-authoring`](skills/skill-authoring/SKILL.md) | Author or fix a nanolama skill: the SKILL.md contract, description formula, cross-refs. |

<!-- END SKILLS TABLE -->

## Creating your first skill

1. Copy [`template/`](template/SKILL.md) to `skills/<your-name>/`.
2. Rewrite the frontmatter and every section (see the [`skill-authoring`](skills/skill-authoring/SKILL.md) skill for the full contract).
3. Add an entry to [`scripts/catalog.yaml`](scripts/catalog.yaml).
4. Regenerate the catalog table and validate:

   ```bash
   python scripts/generate_readme.py
   python scripts/validate_skills.py
   python -m pytest -q
   ```

See [CONTRIBUTING.md](CONTRIBUTING.md) for the full SKILL.md contract.

## Trust & security

Skills are instructions a model will act on. **Only install skill libraries from
sources you trust, and read every `SKILL.md` (and any bundled script) before enabling
it.** In particular, the [`security-review`](skills/security-review/SKILL.md) skill is
*not* hardened against prompt injection — run it only on diffs you trust.

## Portability

nanolama is deliberately model-agnostic. Skills use `entreprise` as the example
tenant name and generic module paths; each one carries an **"Adapt to your repo"**
section telling you what to rename or confirm. Nothing here hard-codes one project's
layout (except the `skill-authoring` skill, which is about *this* repo).

## Contributing & license

Contributions welcome — see [CONTRIBUTING.md](CONTRIBUTING.md). Licensed under
[MIT](LICENSE). Conventions and structure adapted from public skill repos are
credited in [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md).
