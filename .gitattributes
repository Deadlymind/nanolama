# Normalize all text to LF in the repo so the library is byte-consistent across
# platforms (the README sync check and skill parsing assume LF).
* text=auto eol=lf

*.py   text eol=lf
*.md   text eol=lf
*.json text eol=lf
*.yaml text eol=lf
*.yml  text eol=lf
*.txt  text eol=lf
